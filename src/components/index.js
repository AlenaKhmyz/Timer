import React, {useState, useRef} from 'react'

const Timer = () => {


  return (
    <div className="timer">
      <button className="reset"onClick={() => {}}></button>
    </div>
  )
} 

export default Timer;

